import React from "react";
import { SafeAreaView } from "react-native";
import Router from "./Router";

function App() {
	return (
		<SafeAreaView>
			<Router />
		</SafeAreaView>
	);
}

export default App;
